(library
 (name DopsaML_lib)
 (public_name DopsaML.Lib)
 (modules Ast Parser Inferencer Ty Anf Ll Cc Codegen)
 (libraries base angstrom llvm)
 (preprocess
  (pps ppx_expect ppx_deriving.show ppx_deriving.eq))
 (instrumentation
  (backend bisect_ppx))
 (inline_tests))

; (rule
;  (targets runtime.so)
;  (deps runtime.c)
;  (action
;   (run gcc -fPIC -shared %{deps} -o %{targets})))
